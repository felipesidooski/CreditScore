[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "creditscore-pucpr"
version = "0.1.0"
description = "PUCPR advanced modeling project for credit default prediction."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]

